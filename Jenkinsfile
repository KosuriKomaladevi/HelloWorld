@Library('shared-library-github')_
pipeline {
    agent any
    stages {
        stage('Stage1') {
            steps {
                script{
                    sample
                    echo 'shared library working'
                }
            }
        }
    }
}
